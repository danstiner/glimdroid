query ChannelById($id: ID) {
  channel(id: $id) {
    id,
    title,
    category {
        name
    },
    language,
    matureContent,
    status,
    stream {
        id,
        countViewers,
        peakViewers,
        startedAt,
        thumbnailUrl
    },
      streamer {
          id,
          avatarUrl,
          displayname,
          username
      }

      chatMessages(last: 10) {
          edges {
              node {
                  id
                  message
                  insertedAt
                  channel {
                      status
                  }
                  user {
                      displayname
                      username
                      avatarUrl
                  }
              }
          }
      }

      subcategory {
          name
      },
      tags {
          name
      },
  }
}
